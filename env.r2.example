# Cloudflare R2 Storage Configuration
# Copy these to your .env.local file and fill in the actual values

# R2 API Credentials (get from Cloudflare Dashboard > R2 > Manage R2 API Tokens)
CLOUDFLARE_R2_ACCESS_KEY_ID=your_access_key_id_here
CLOUDFLARE_R2_SECRET_ACCESS_KEY=your_secret_access_key_here

# R2 Bucket Configuration
CLOUDFLARE_R2_BUCKET_NAME=eduprima-tutor-files

# R2 Endpoint (replace YOUR_ACCOUNT_ID with actual Cloudflare account ID)
CLOUDFLARE_R2_ENDPOINT=https://YOUR_ACCOUNT_ID.r2.cloudflarestorage.com

# Public URL for file access
# Option 1: Custom domain (recommended)
CLOUDFLARE_R2_PUBLIC_URL=https://cdn.eduprima.com

# Option 2: R2 public URL (if no custom domain)
# CLOUDFLARE_R2_PUBLIC_URL=https://pub-xxxxxxxxxxxxxxxxx.r2.dev

# How to get these values:
# 1. Login to Cloudflare Dashboard
# 2. Go to R2 Object Storage
# 3. Create bucket and get bucket name
# 4. Create API Token with R2 read/write permissions
# 5. Get account ID from dashboard (bottom right)
# 6. Setup custom domain in bucket settings (optional)

# For development/testing, you can use these placeholder values:
# CLOUDFLARE_R2_ACCESS_KEY_ID=test_key_id
# CLOUDFLARE_R2_SECRET_ACCESS_KEY=test_secret_key
# CLOUDFLARE_R2_BUCKET_NAME=test-bucket
# CLOUDFLARE_R2_ENDPOINT=https://test-account.r2.cloudflarestorage.com
# CLOUDFLARE_R2_PUBLIC_URL=https://test.example.com